# Tasks: xiaomi-wifi-provisioning

> 前置：openspec change `xiaomi-control-panel` 需先真机验收并提交（本变更复用它的
> HTTP 服务与页面）。

## 1. 凭据存储（纯逻辑先行，可 native 测试）

- [ ] 1.1 `wifi_store.h`：凭据表的纯逻辑部分——槽位增删改查、5 组上限、
      重复 SSID 覆盖而非追加、种子只灌一次的标记语义（Arduino-free）
- [ ] 1.2 native 单元测试：满槽报错、同名覆盖、删除后种子不复活、非法输入拒绝
- [ ] 1.3 `wifi_store.cpp`：NVS 持久化（键 `wn0..wn4` + `wseed`），
      迁移逻辑（首次启动灌入编译期种子）

## 2. 多网络连接

- [ ] 2.1 用 `WiFiMulti` 替换 `wifiEnsureUp` 在语音固件中的调用路径：加载所有已存
      网络 → `run()` → 15s 预算；**不改动 buddy 固件共用的 wifi_stream.cpp**
- [ ] 2.2 各阶段屏幕字幕反馈（找网络中 / 正在连 X / 已连上 + 面板地址 / 失败原因）
- [ ] 2.3 真机验证：存两个网络，切换环境后自动连上正确的那个

## 3. 面板网络管理

- [ ] 3.1 `GET /api/networks`（只回 SSID + 当前连接项，**无密码**）
- [ ] 3.2 `POST /api/networks` 添加/更新、`DELETE /api/networks` 删除，满槽明确报错
- [ ] 3.3 `GET /api/networks/scan` 扫描附近网络（SSID/RSSI/加密），在 HTTP 任务中执行
- [ ] 3.4 `panel.html` 新增"网络"区块：已存列表 + 扫描点选 + 添加/删除
- [ ] 3.5 验证：密码不回显；扫描时对话 underrun 仍为 0

## 4. 热点配网兜底

- [ ] 4.1 `wifi_setup_ap.{h,cpp}`：连接全失败 → 停 STA → `softAP("小咪-setup", 密码)`
      + `DNSServer` 全解析到自身
- [ ] 4.2 配网页（复用面板 WebServer 换路由）：扫描 → 选网络 → 输密码 → 保存
- [ ] 4.3 未知路径一律 302 到配网页（覆盖各系统门户探测行为不一致）
- [ ] 4.4 屏幕显示热点名 + 密码；超时（默认 10 分钟）无人配置则关热点回打盹
- [ ] 4.5 配网成功后不重启，直接连上新网络并回到正常流程

## 5. 真机验收

- [ ] 5.1 通勤场景：家里存好两个网络 → 换到另一网络环境唤醒 → 自动连上
- [ ] 5.2 面板管理：加/删/扫描均生效，密码从不回显
- [ ] 5.3 配网兜底：在无已知网络环境唤醒 → 屏幕给指引 → 手机连热点自动弹页 →
      配好即用（不重启）
- [ ] 5.4 门户兜底：手机未自动弹页时，手动访问任意网址能跳转到配网页
- [ ] 5.5 超时收场：进配网模式后放置到超时，热点自动关闭
- [ ] 5.6 回归：对话链路不受影响（underruns=0）；buddy 固件编译不受影响
- [ ] 5.7 `make test` 全绿；README 补配网小节（含访客网络隔离的说明）
- [ ] 5.8 用户 review + commit + push
