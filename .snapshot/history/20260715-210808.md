# 会话交接书 — 2026-07-15 21:07  [源:cheap-model]
> 触发:SessionEnd (prompt_input_exit)  |  session:ab45df9e-fd8b-4018-989d-13502941e4b0

## 目标
启动四个 bridge daemon（cc-bridge, cursor-bridge, codex-bridge, opencode-bridge）并连接 cardputer 设备到 Mac，解决 BLE 连接不稳定问题。

## 已完成
- ✅ 四个 bridge daemon 全部启动（launchd 托管）
- ✅ cursor-bridge `no_ble` 配置生效，不抢 BLE 射频
- ✅ cardputer (Claude-7AFD) BLE 连接稳定化：通过完全硬断电 → 5 秒 → 重启，设备侧 BLE 栈彻底重建
- ✅ 连接验证：14:53:32–14:56:18 连续稳定 ~2.75 分钟，零掉线、零 Service Discovery 失败

## 当前状态
| 组件 | 状态 | 指标 |
|---|---|---|
| cc/cursor/codex/opencode bridge | ✅ 运行 | launchd 托管 |
| cardputer (7AFD) | ✅ 已连稳 | `subscribed` 计数=5（自 14:53 后保持），`sdfail` 停止增长 |
| 状态推送 | ✅ 正常 | 每帧 emit 正好 3 条 skip（对应离线的 SC-/RC-/F7C2 三个幻影设备），7AFD 不 skip |

## 改动的文件
**无代码改动**。调试操作日志：
- `~/Library/Logs/cc-bridge.err.log` — bridge daemon 日志，反复记录连接过程

## 未决问题/下一步
1. **cursor-bridge TypeError**（低优先级，不影响当前功能）
   - 发生在 Cursor Stop 事件，`add_entry() max_len` 相关
   - 建议：可选后续修复

2. **幻影设备 skip 噪音**（可忽略）
   - stick/rover/F7C2 未上电，每帧产生 3 条 `write skipped: not connected`
   - 原因：cc-bridge 往所有已配置 peer 写，缺席的标记 skip
   - 影响：日志噪音，无功能问题

## 关键约束与坑
1. **BLE 栈卡死恢复**
   - ❌ 软重启（按键）无效
   - ❌ Mac 蓝牙栈重启无效
   - ✅ **完全硬断电** → 等 5 秒 → 重新上电（设备侧 BLE 栈彻底重建）
   - 症状：`Service Discovery has not been performed yet` 反复循环、`subscribed` 计数不增

2. **连接稳定性判断标准**
   - 只信 `subscribed to NUS TX` 计数，不信启发式（如"能 scan 到"）
   - 稳定 = `subscribed` 保持不变 + `sdfail` 停止增长 + 无 `write skipped` 针对该 peer
   - 第一次 subscribe 后立即掉线 = 仍是半卡死，不是连稳

3. **cursor-bridge 射频竞争**
   - 必须配 `no_ble` 标志防止 cursor-bridge 扫 BLE
   - 症状：cursor-bridge 正在扫时 cardputer 连接被冻结
   - 配置位置：cursor-bridge 的 launchd plist 或启动参数

4. **daemon 重启**
   - cc-bridge: `launchctl unload` + `load`（不能用 `kickstart -k`）
   - 改环境变量后必须 unload 重新读取 plist

5. **多 peer 管理**
   - `write skipped` 计数 = 该 emit 后未连的 peer 数
   - 识别设备用 BT MAC（Tab5/cardputer 有固定 MAC），不用 USB 序列号（会翻转）

_由 session-handoff 生成;接班 agent 从此文件开始。_
